// Game will be played with ships of these lengths
export const SHIP_LENGTHS = [5, 4, 3, 3, 2];

// AI move delay in milliseconds
export const AI_DELAY = 250;

// the size of the gameboard
export const EDGE_SIZE = 10;
